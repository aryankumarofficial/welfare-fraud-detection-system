name: Build and Push Docker Images

on:
  push:
    branches: [main]

jobs:
  docker:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Admin Image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/admin -f apps/admin/Dockerfile .
          docker push ghcr.io/${{ github.repository }}/admin

      - name: Build API Image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/api ./apps/api
          docker push ghcr.io/${{ github.repository }}/api

      - name: Build ML Image
        run: |
          docker build -t ghcr.io/${{ github.repository }}/ml ./services/ml
          docker push ghcr.io/${{ github.repository }}/ml
