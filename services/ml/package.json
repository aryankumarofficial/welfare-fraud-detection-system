{
  "name": "@repo/ml-service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uvicorn src.app:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn src.app:app --host 0.0.0.0 --port 8000",
    "install:py": "pip install -r requirements.txt",
    "validate:e2e": "python src/validate_end_to_end.py"
  }
}
